import { useState } from "react"


export default function ComptiaAPlusPorts() {

  const COMPTIA_PORTS_QA = {
    protocol: ["FTP", "SSH", "Telnet", "SMTP", "DNS", "DHCP", "HTTP", "POP3", "NetBIOS", "IMAP", "SNMP", "LDAP", "HTTPS", "SMB", "RDP"]
  }

  return(
    <div className="flex items-center justify-center w-full h-full">
      
    </div>
  )
}